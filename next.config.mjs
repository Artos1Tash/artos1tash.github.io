/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/artos1tash',
  assetPrefix: '/artos1tash/',
};

export default nextConfig;